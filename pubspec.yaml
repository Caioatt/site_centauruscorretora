name: seu_projeto
description: Um aplicativo Flutter para enviar e-mails usando SMTP.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  responsive_builder: ^0.7.0  
  http: ^1.2.1
  google_fonts: ^6.2.1
  extended_masked_text: ^2.3.1
  url_launcher: ^6.2.5
  flutter_masked_text2: ^0.9.1
  carousel_slider: ^4.0.0
  clipboard: ^0.1.3
  #flutter_html: ^2.2.1
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/assets/imgs/